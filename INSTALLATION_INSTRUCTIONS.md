# הוראות התקנה - Moodle PDF Downloader v1.3.1

## 📦 התקנה מהקובץ המארז (ZIP)

### שלב 1: פתיחת הקובץ המארז
1. פתח את הקובץ `Moodle_Downloader_Extension_v1.3.1.zip`
2. חלץ את כל הקבצים לתיקייה חדשה (למשל: `Moodle_Downloader_Extension`)

### שלב 2: התקנת התוסף ב-Chrome
1. פתח את Google Chrome
2. עבור לכתובת: `chrome://extensions/`
   - לחילופין: לחץ על `⋮` (שלוש נקודות) → `כלים נוספים` → `הרחבות`
3. הפעל "מצב מפתח" (Developer mode) בפינה הימנית העליונה
4. לחץ על "טען תוסף לא ארוז" (Load unpacked)
5. בחר את התיקייה `Moodle_Downloader_Extension` (התיקייה שבה נמצאים כל הקבצים)

### שלב 3: בדיקה
- ודא שהתוסף מופיע ברשימת ההרחבות
- ודא שהאייקון מופיע בסרגל הכלים של Chrome
- לחץ על האייקון כדי לבדוק שהחלון הקופץ נפתח

## ✅ רשימת קבצים נדרשים

התיקייה חייבת להכיל את הקבצים הבאים:

### קבצים ראשיים:
- ✅ `manifest.json` - קובץ ההגדרות הראשי
- ✅ `popup.html` - ממשק המשתמש
- ✅ `popup.js` - לוגיקת הפופ-אפ
- ✅ `content.js` - סקריפט תוכן לסריקת הדף
- ✅ `utils.js` - פונקציות עזר
- ✅ `background.js` - Service worker
- ✅ `styles.css` - עיצוב
- ✅ `guide.html` - מדריך שימוש מפורט

### קבצים נוספים (אופציונליים):
- `icon-generator.html` - כלי ליצירת אייקונים
- `LICENSE` - רישיון MIT
- `README.md` - תיעוד
- `CHANGELOG.md` - רשימת שינויים

### תיקיית אייקונים:
- ✅ `icons/icon16.png` (16x16 פיקסלים)
- ✅ `icons/icon48.png` (48x48 פיקסלים)
- ✅ `icons/icon128.png` (128x128 פיקסלים)

## 🔧 פתרון בעיות

### התוסף לא נטען
- ודא שכל הקבצים נמצאים בתיקייה הנכונה
- ודא שיש תיקיית `icons` עם 3 קבצי PNG
- בדוק את ה-Console ב-`chrome://extensions/` לשגיאות

### האייקון לא מופיע
- ודא שקיימים 3 קבצי PNG בתיקיית `icons`
- ודא שהקבצים נקראים: `icon16.png`, `icon48.png`, `icon128.png`
- נסה לרענן את התוסף (לחץ על כפתור הרענון)

### התוסף לא עובד
- ודא שאתה בדף קורס למדא תקין (lemida.biu.ac.il/course/)
- ודא שהדף נטען במלואו
- פתח את ה-Console (F12) ובדוק אם יש שגיאות

## 📧 תמיכה

לפניות תמיכה, שאלות או דיווח על באגים:
- **מייל:** idan.vaknin1@live.biu.ac.il
- **מפתח:** Idan Vaknin

## ⚠️ הצהרת אחריות

התוסף מסופק "כפי שהוא" ללא כל אחריות. השימוש בתוסף הוא על אחריות המשתמש בלבד.

לקריאת הצהרת האחריות המלאה, ראה את הקובץ `README.md` או `guide.html`.

---

**גרסה:** 1.3.1  
**תאריך:** 2025-11-24  
**© Idan Vaknin**


